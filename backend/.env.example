# Server
PORT=3000

# PostgreSQL (dinocamp database)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_password_here
PGDATABASE=dinocamp

# Or use a single connection URL instead of the variables above:
# DATABASE_URL=postgresql://postgres:your_password_here@localhost:5432/dinocamp
